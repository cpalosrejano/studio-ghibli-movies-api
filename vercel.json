{
    "builds": [
        {
            "src": "main.py",
            "use": "@vercel/python"
        }
    ],
    "buildCommand": "pybabel compile -f -d translations",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "main.py"
        }
    ]
}
